{
  "name": "@hydra/cloud",
  "version": "3.0.0",
  "description": "HYDRA cloud — auth + settings sync",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "start": "NODE_ENV=production node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^5.2.0",
    "pino-pretty": "^13.1.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
